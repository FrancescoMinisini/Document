\begin{tikzpicture}[
    x=1cm,y=1cm,
    >=Latex,
    font=\small,
    bluenode/.style={
        circle,
        minimum size=0.42cm,
        inner sep=0pt,
        draw=blue!70!black,
        fill=blue!8
    },
    greennode/.style={
        circle,
        minimum size=0.42cm,
        inner sep=0pt,
        draw=green!60!black,
        fill=green!10
    },
    envbox/.style={
        draw=gray!60!black,
        fill=gray!6,
        rounded corners,
        thick,
        inner sep=10pt
    },
    trainbox/.style={
        draw=orange!80!black,
        rounded corners,
        densely dashed,
        thick,
        inner sep=8pt
    },
    statebox/.style={
        draw=blue!70!black,
        rounded corners,
        thick,
        inner sep=6pt,
        fill=blue!2
    },
    updatebox/.style={
        draw=gray!60!black,
        rounded corners,
        thick,
        inner sep=6pt,
        fill=gray!4
    },
    arr/.style={-{Latex[length=2.2mm]}, thick},
    bluearr/.style={-{Latex[length=2.2mm]}, thick, blue!80!black},
    orangearr/.style={-{Latex[length=2.2mm]}, thick, orange!85!black},
    infarr/.style={-{Latex[length=2.2mm]}, thick, blue!80!black, densely dashed},
    updarr/.style={-{Latex[length=2.2mm]}, thick, orange!85!black, densely dashed}
]

% -------------------------------------------------
% POLICY STATE
% -------------------------------------------------
\node[statebox, align=center] (state) at (4.4,0.2)
    {\textbf{State} $\, s_t = (U_t,t)$};

% Policy NN nodes (layer 1 - input layer)
\node[bluenode] (p11) at (3.2,2.1) {};
\node[bluenode] (p12) at (4.0,2.1) {};
\node                   at (4.8,2.1) {$\dots$};
\node[bluenode] (p13) at (5.6,2.1) {};

% Policy NN nodes (layer 2 - hidden layer)
\node[bluenode] (p21) at (2.8,3.0) {};
\node[bluenode] (p22) at (3.6,3.0) {};
\node                   at (4.4,3.0) {$\dots$};
\node[bluenode] (p23) at (5.2,3.0) {};
\node[bluenode] (p24) at (6.0,3.0) {};

% Policy NN nodes (layer 3 - output layer)
\node[bluenode] (p31) at (3.2,3.9) {};
\node[bluenode] (p32) at (4.0,3.9) {};
\node           (p3dots) at (4.8,3.9) {$\dots$};
\node[bluenode] (p33) at (5.6,3.9) {};

% Policy connections
\begin{pgfonlayer}{background}
\foreach \i in {1,2,3}
    \foreach \j in {1,2,3,4} {
        \draw[gray!40, thin] (p1\i) -- (p2\j);
    }
\foreach \i in {1,2,3,4}
    \foreach \j in {1,2,3} {
        \draw[gray!40, thin] (p2\i) -- (p3\j);
    }
\end{pgfonlayer}

% Policy NN brace (right side)
\draw[decorate, decoration={brace, amplitude=6pt, mirror}, thick, black!70!black]
    (6.3,1.85) -- (6.3,4.15)
    node[midway,right=0.25cm,align=left,text=black!80!black] {
        \textbf{Policy NN} $\pi_\theta$\\
        \textnormal{\scriptsize Input: $s_t$}\\
        \textnormal{\scriptsize Output: $\mu_\theta(s_t),\,\Sigma_\theta$}
    };

% State to policy
\draw[bluearr] (state.north) -- (4.4,1.85);

% -------------------------------------------------
% ACTION + ENVIRONMENT (centered above Policy NN)
% -------------------------------------------------
\node[align=center] (action) at (4.4,5.1)
    {\textbf{Action} $\; a_t \in \mathbb{R}^m$};

% Arrows from top of Policy NN going upward to Action
\draw[bluearr] (p32) -- (action);
\draw[bluearr] (p3dots) -- (action);

\node[align=center] (H) at (4.4,6.5) {$\hat{H}_{t+1}$};
\node[align=center] (U) at (4.4,7.75)
    {$U_{t+1}=\exp\!\left[-i\hat{H}_{t+1}\,\delta t\right]U_t$};
\node[align=center] (cost) at (4.4,9.1)
    {\textbf{Cost} $c_t$ \ \& \textbf{Next State} $(U_{t+1},t+1)$};

% Noise injection (compact annotation between action and H)
% \node[font=\scriptsize, text=red!70!black, align=center] (noise) at (6.2,5.8)
%     {\textit{if noise:}\\ $a_t + \epsilon_t$};
\draw[arr, gray!75!black] (action.north) -- (H.south);
% \draw[-{Latex[length=1.8mm]}, densely dotted, red!70!black, thin]
%     (noise.west) -- (4.9,5.8);
\draw[arr, gray!75!black] (H.north) -- (U.south);
\draw[arr, gray!75!black] (U.north) -- (cost.south);

\begin{pgfonlayer}{background}
\node[envbox, fit=(H)(U)(cost), inner xsep=0.5cm, inner ysep=0.45cm] (env) {};
\end{pgfonlayer}

% Environment brace (anchored to env box)
\draw[decorate, decoration={brace, amplitude=6pt}, thick]
    ([xshift=-1.0cm]env.south west) -- ([xshift=-1.0cm]env.north west)
    node[midway,left=0.35cm,align=center] {\large \textbf{Environment}};

% -------------------------------------------------
% TRAINING BLOCK (RIGHT)
% -------------------------------------------------
% Training title (COMMENTED OUT)
% \node[font=\large\bfseries, text=orange!90!black] (trainingtitle) at (17.0,10.0) {TRAINING};

% Value NN nodes (layer 1 - input layer)
\node[greennode] (v11) at (14.7,8.8) {};
\node[greennode] (v12) at (15.5,8.8) {};
\node                    at (16.3,8.8) {$\dots$};
\node[greennode] (v13) at (17.1,8.8) {};
\node[greennode] (v14) at (17.9,8.8) {};

% Value NN nodes (layer 2 - hidden layer)
\node[greennode] (v21) at (14.7,7.9) {};
\node[greennode] (v22) at (15.5,7.9) {};
\node                    at (16.3,7.9) {$\dots$};
\node[greennode] (v23) at (17.1,7.9) {};
\node[greennode] (v24) at (17.9,7.9) {};

% Value NN nodes (layer 3 - output layer)
\node[greennode] (v31) at (15.1,7.0) {};
\node[greennode] (v32) at (15.9,7.0) {};
\node                    at (16.7,7.0) {$\dots$};
\node[greennode] (v33) at (17.5,7.0) {};

% Value NN connections
\begin{pgfonlayer}{background}
\foreach \i in {1,2,3,4}
    \foreach \j in {1,2,3,4} {
        \draw[gray!40, thin] (v1\i) -- (v2\j);
    }
\foreach \i in {1,2,3,4}
    \foreach \j in {1,2,3} {
        \draw[gray!40, thin] (v2\i) -- (v3\j);
    }
\end{pgfonlayer}

% Value NN brace (left side)
\draw[decorate, decoration={brace, amplitude=6pt}, thick, black!60!black]
     (14.4,6.75) -- (14.4,9.05)
    node[midway,left=0.25cm,align=right,text=black!60!black] {
        \textbf{Value NN} $V_\omega$\\
        \textnormal{\scriptsize where $\omega$ approximate $ V_{\pi_{\mathrm{old}}} $} \\
        \textnormal{\scriptsize Input: $s_{t}$}\\
        \textnormal{\scriptsize Output: $\in \mathbb{R}$}
    };


% -------------------------------------------------
% LOOPS FROM ENVIRONMENT TOP
% Both arrows start from env.north, go up, then split left/right
% -------------------------------------------------

% Inference loop: blue arrow from env.north, going left past NW corner, down to state
\draw[infarr, rounded corners=10pt]
    (env.north) -- ([yshift=0.5cm]env.north)
    -- ([xshift=-0.2cm, yshift=0.5cm]env.north west)
    -- ([xshift=-0.2cm]env.south west |- {(0,0.2)})
    -- (state.west);
\node[text=blue!80!black, font=\scriptsize\bfseries, anchor=east]
    at ([xshift=-0.3cm, yshift=.7cm]env.north)
    {Inference loop: $s_t \leftarrow s_{t+1}$};

% Training loop: orange arrow from env.north, going right to Value NN input
\draw[orangearr, rounded corners=8pt]
    (env.north) 
    -- ([yshift=0.5cm]env.north)
    % -- ([yshift=0.5cm]env.north east) -- ++(0.5,0.3)
    -| (15.9,9.3) -- (15.9,8.95);
\node[text=orange!90!black, font=\scriptsize\bfseries, anchor=west]
    at ([xshift=0.6cm, yshift=0.7cm]env.north)
    {Training loop (once per batch): $ \mathcal{B} = \left\{(s_t, a_t, c_t, s_{t+1})\right\}_{t=0}^{T-1}$};

% Value function output + definition
\node[align=center] (vdef) at (15.9,5.8)
    % {$V_\omega(s_{t+1}) \approx \mathbb{E}\!\left[G_{t+1} \mid s_{t+1}\right]$};
    {Expected Cost under $ \pi_{\theta_{\mathrm{old}}} $ : $V_\omega(s_{t})$};
\draw[orangearr] (v32.south) -- (vdef.north);

% advantage formula
\node[align=center] (adv) at (15.9,4.6)
    % {$\hat{A}_t = r_t + \gamma\,V_\omega(s_{t+1}) - V_\omega(s_t)$};
    {Advantage: $\hat A_t =
c_t+\gamma V_{\omega_{\mathrm{old}}}(s_{t+1}) - V_{\omega_{\mathrm{old}}}(s_t)$};
\draw[orangearr] (vdef.south) -- (adv.north);

% --- Value NN weight update (to the left, parallel with policy update) ---

% Arrow from Value NN output directly to loss
\node[align=center, font=\scriptsize, text=black!90!black] (vloss) at (11.0,4.8)
    {Value Loss: $ L_\omega $ };
\draw[updarr,rounded corners=8pt ] (adv.south) -- ([xshift=-.5cm, yshift=-.3cm]adv.south) -- ([yshift=-.6cm]vloss.south) -- (vloss.south);

% Arrow from loss back to Value NN output (v31), with omega update label on the line
\draw[updarr, rounded corners=8pt] (vloss.north) -- ( [yshift=1.2cm]vloss.north) -- ([xshift=-.7cm, yshift=-.6cm]v31.south west) -- (v31.south west)
    node[midway, left=1cm, text=orange!90!black, font=\scriptsize]
    { Update: $ V_{\pi_{\mathrm{old}}} \leftarrow  V_{\pi} $  or $\omega \leftarrow \omega'$};


% Policy update (TRPO optimization formulas)
\node[updatebox, align=center, font=\scriptsize] (update) at (15.9,2.8)
    {$ \theta^\prime = \max_\theta \;\mathbb{E}\!\left[\dfrac{\pi_\theta(a_t\mid s_t)}{\pi_{\theta_{\mathrm{old}}}(a_t\mid s_t)}\,\hat{A}_t\right]$\\[0.3em]
    s.t.\ $\mathbb{E}\!\left[D_{\mathrm{KL}}\!\left(\pi_{\theta_{\mathrm{old}}}\,\|\,\pi_\theta\right)\right]\leq\delta$};
\draw[orangearr] (adv.south) -- (update.north);

% TRPO Optimization brace (left side of update box)
\draw[decorate, decoration={brace, amplitude=5pt}, thick, black!80!black]
     ([xshift=-0.15cm]update.south west) -- ([xshift=-0.15cm]update.north west) 
    node[midway,left=0.20cm,align=right,text=black!80!black,font=\small] {
        \textbf{TRPO:} Trust Region \\ Policy Optimization
    };

% dashed update arrow to policy (goes down, right under NN, then up to p24.east)
\draw[updarr, rounded corners=10pt]
    (update.south) -- ++(0,-0.6) -- ++(-9.1,0)
    node[midway,below,text=orange!90!black] {Update: $\pi_\theta \leftarrow \pi_\theta^{new}$ or $\theta \leftarrow \theta'$}
    -- (p13.south east);


% -------------------------------------------------
% INFERENCE BOX (blue, no fill, behind everything)
% -------------------------------------------------
\begin{pgfonlayer}{background}
\draw[blue!70!black, thick , dotted, rounded corners=8pt, opacity= 0.6]
    ([xshift=-2.5cm, yshift=-0.2cm]state.south west)
    rectangle
    ([xshift=1.4cm, yshift=1.2cm]env.north east);
\end{pgfonlayer}
\node[font=\bfseries, text=blue!70!black, anchor=south west]
    at ([xshift=-1.5cm, yshift=1.05cm]env.north)
    {\large Inference Loop};

% -------------------------------------------------
% TRAINING BOX (orange, no fill, behind everything)
% -------------------------------------------------
\begin{pgfonlayer}{background}
\draw[orange!80!black, thick, dotted, rounded corners=8pt, opacity= 0.6]
    ([xshift=-1.5cm, yshift=-1.4cm]update.south west)
    rectangle
    ([xshift=0.6cm, yshift=1.0cm]env.north -| v14.east);
\end{pgfonlayer}
\node[font=\bfseries, text=orange!80!black, anchor=south east]
    at ([xshift=0.5cm, yshift=1.05cm]env.north -| v14.east)
    {\large Training Loop};

% -------------------------------------------------
% AGENT BRACE (left, aligned with environment brace)
% -------------------------------------------------
\draw[decorate, decoration={brace, amplitude=7pt},  thick, black!70!black]
    ([xshift=-1cm]env.south west |- {(0,0.1)}) -- ([xshift=-1cm]env.south west |- {(0,5.5)})
    node[midway,left=0.35cm,text=black!80!black] {\Large \textbf{Agent}};

\end{tikzpicture}